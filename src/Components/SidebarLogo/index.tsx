import React from "react";
import styles from "./index.module.scss";

const SidebarLogo = () => {
    return (
        <div className={styles.logo_wrapper}>
            <p className={styles.logo}>6rBot</p>
        </div>
    )
}

export default SidebarLogo;